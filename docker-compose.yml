version: "3.9"

services:

  backend:
    build: ./backend
    container_name: pulsecheck-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: pulsecheck-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
